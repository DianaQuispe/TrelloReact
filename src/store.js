
import createStore from 'redux-zero'
const BOARDS =
 [
    {
        each: {
        [lp],
        [lp],
        [lp]
                }
    }
 ]

const initialState = {
    algo: 1
}

const store = createStore (initialState)
export default store